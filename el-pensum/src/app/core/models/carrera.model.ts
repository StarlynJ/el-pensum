// Modelo para una carrera (ej: Ingeniería, Medicina, etc)
export interface Carrera {
  id?: number; // Id único (opcional)
  nombre: string; // Nombre de la carrera
  iconoUrl?: string; // URL del ícono (opcional)
}